[project]
name = "1001website"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
  'django',
  'dj-database-url',
  'gunicorn',
  'psycopg[binary]',
  'whitenoise',
  "markdown>=3.10.2",
]

[dependency-groups]
dev = []
